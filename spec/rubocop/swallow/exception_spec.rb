require "spec_helper"

describe Rubocop::Swallow::Exception do
  it "has a version number" do
    expect(Rubocop::Swallow::Exception::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
